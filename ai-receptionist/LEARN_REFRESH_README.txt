Learning Center content refresh — Batch 1 of N
================================================
Unzip at the ROOT of your project (the folder with src/ and public/).

ONE FILE changes: public/js/learn.js
NO database migration. NO backend change. NO schema. Content + light render tweak only.

THIS BATCH:
- Image placeholders ("[Screenshot: ...]") no longer appear ANYWHERE — the
  renderer now skips them, so every guide flows without empty frames.
- The single "Setting up an automation" guide is REPLACED by 9 real, accurate
  Automations guides (overview, triggers, conditions, actions, compute fields,
  scheduling & delays, webhooks, start from a template, build with a wizard).

STILL TO COME (later batches, with plain-English summaries to review):
- Accuracy pass over the other categories (Getting started, Contacts, Finding &
  organizing, Bulk actions, Importing & exporting, Custom fields, Reports,
  Recycle Bin, Appearance, Your account) and physical removal of their now-hidden
  placeholder entries.

See the chat for restore-point, run, and revert commands, plus the per-guide
plain-English summary of what each new Automations guide says.
