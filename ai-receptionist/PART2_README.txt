PART 2: Candidate dropdown now shows email + phone under each name
==================================================================
Unzip at the project ROOT. One file: public/js/portal.js.
NO migration, NO schema change. Display-only — does not change what the search
queries or how linking writes the RecordLink.

Each result in the "Link a contact" box now shows the contact's name (bold) with
their email · phone on a smaller line beneath, so two contacts with the same
name can be told apart before linking.
